## Packages
framer-motion | For smooth animations and page transitions
react-player | To display the YouTube video preview
date-fns | For formatting dates relative to now

## Notes
- The frontend needs to poll the job status every few seconds when a job is 'pending' or 'processing'.
- Font choices: 'Outfit' for headings (modern, clean) and 'Inter' for body text (readable).
